<br>
<div class="jumbotron">
  <h1>Sign Up Confirmation</h1>
  <p>Are you sure these values are correct?</p>
  <h4>email: <%= @user.email %></h3>
  <h4>name: <%= @user.name %></h3>

<div class="row">
  <div class="col-md-4">
    <div>
      <%= button_to "Yes", {controller: "users", action: "create", params: params}, class: 'btn btn-success' %>
    </div>
    <div>
      <%= button_to "No", new_user_path(@user), class: 'btn btn-failure' %>
    </div>
  </div>
</div>
